import{useState}from'react';import{useNavigate}from'react-router-dom';import{Card,Form,Input,Button,message}from'antd';import{UserOutlined,LockOutlined}from'@ant-design/icons';import{adminApi}from'../../services/api';import{useAuthStore}from'../../store/authStore';
export default function AdminLogin(){const[loading,setLoading]=useState(false);const nav=useNavigate();const setAuth=useAuthStore(s=>s.setAuth);
const submit=async v=>{setLoading(true);try{const r=await adminApi.login(v);setAuth(r.accessToken,r.admin);message.success('登录成功');nav('/admin/dashboard')}catch(e){message.error(e.message)}finally{setLoading(false)}};
return(<div style={{minHeight:'100vh',display:'flex',alignItems:'center',justifyContent:'center',background:'#f1f5f9'}}><Card style={{width:380,borderRadius:12}} title={<div style={{textAlign:'center',fontSize:16}}>蓝域出海 · 管理后台</div>}><Form onFinish={submit} size="large"><Form.Item name="username" rules={[{required:true}]}><Input prefix={<UserOutlined/>} placeholder="用户名"/></Form.Item><Form.Item name="password" rules={[{required:true}]}><Input.Password prefix={<LockOutlined/>} placeholder="密码"/></Form.Item><Form.Item><Button type="primary" htmlType="submit" block loading={loading} style={{borderRadius:10,height:44}}>登录</Button></Form.Item></Form></Card></div>)}
